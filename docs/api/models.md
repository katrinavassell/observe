# Models API

## Aggregate by Model

```
GET /api/events/by-model
```

Returns cost and volume data grouped by AI model. Only includes events where the `model` field is populated.

See [Events API — Aggregate by Model](./events.md#aggregate-by-model) for the full reference.
